{
  "name": "koteika",
  "version": "1.0.0",
  "description": "Cats hotel",
  "main": "index.js",
  "scripts": {
    "test": "hi gleb",
    "check": "prettier --check './**/*.{html,css,js}'",
    "format": "prettier --write './**/*.{html,css,js}'",
    "lint": "stylelint '**/*.css'",
    "lintfix": "stylelint '**/*.css' --fix",
    "checkAll": "npm run check && npm run lint",
    "formatAll": "npm run format && npm run lintfix",
    "build": "gulp build",
    "dev": "gulp",
    "fixAndBuild": "npm run fixAll && npm run build"
  },
  "author": "Yandex Practikum students",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^3.0.2",
    "del": "^6.0.0",
    "gulp": "^5.0.0",
    "gulp-concat-css": "^3.1.0",
    "gulp-plumber": "^1.2.1"
  }
}
